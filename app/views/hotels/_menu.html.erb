<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
  <div class="container-fluid">
    <%= link_to "Hotels", "/", class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0"> <!-- Added ms-auto here -->
        <% if user_signed_in? %>
          <% if !controller.action_name.in?(%w[new create]) %>
            <li class="nav-item">
              <%= link_to "Reservations", user_reservations_path(current_user), class: "nav-link" %>
            </li>
          <% end %>
          <% if current_user.role == "admin" %>
            <li class="nav-item">
              <%= link_to "Admin Panel", admin_dashboard_index_path, class: "nav-link" %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "Logout", users_sign_out_path, turbo_method: :delete, class: "nav-link btn btn-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-outline-primary" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
